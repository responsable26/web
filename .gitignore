# Archivos basura de macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Basura de otros SO / editores
Thumbs.db
ehthumbs.db
Desktop.ini

# Dependencias (por si acaso en la raíz)
node_modules/

# Variables de entorno con secretos (la key de Resend). Solo se versiona .env.example
.env
.env.local
.env.*.local
.vercel/
