{
  "name": "responsable-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Pruebas de humo (Playwright) para las páginas de servicio de ResponSable",
  "scripts": {
    "test": "node modal-smoke.js",
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
